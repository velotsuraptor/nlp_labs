# Full data access

The notebook first looks for `project_lab11/data/processed_v2/processed_v2.csv`.
If the file is absent, it falls back to `project_lab2/data/processed_v2/processed_v2.csv`.
This repo keeps only a curated sample in `data/sample/` for lightweight sharing.
